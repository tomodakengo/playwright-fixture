node_modules/
test-results/
playwright-report/
playwright/.cache/
blob-report/

# Auth state files generated by tests/auth.setup.ts (and the demo equivalent)
tests/.auth/*.json
!tests/.auth/.gitkeep
examples/tests/.auth/*.json
!examples/tests/.auth/.gitkeep

# Env
.env
.env.local
.env.*.local

# OS / IDE
.DS_Store
Thumbs.db
*.log

# Local Claude settings (per-user, not shared)
.claude/settings.local.json

# Build output
dist/
build/

# Docker (per-developer compose overrides; see docker-compose.yml for the committed base)
docker-compose.override.yml
.docker-cache/

# Devcontainer scratch directories
.devcontainer/.cache/
